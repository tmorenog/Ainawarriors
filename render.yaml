services:
  - type: web
    name: warriors-clans-relay
    env: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    healthCheckPath: /health
    envVars:
      - key: CORS_ORIGIN
        value: "*"
